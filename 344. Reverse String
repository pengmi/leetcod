class Solution {
public:
    string reverseString(string s) {
        int length=s.length();
        for(int i=0,j=length-1;i<length/2;)
        {
            swap(s[i++],s[j--]);
        }
        return s;
    }
};
